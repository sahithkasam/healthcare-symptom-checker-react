# Environment variables for the Healthcare Symptom Checker API

# OpenAI API Configuration (optional - will use mock responses if not provided)
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=8000
NODE_ENV=development

# Database Configuration
DB_PATH=./symptom_history.db

# CORS Configuration
CORS_ORIGIN=http://localhost:3000